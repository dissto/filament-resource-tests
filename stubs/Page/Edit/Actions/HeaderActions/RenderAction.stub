it('can render action on the edit page', function (string $action) {
   livewire(List{{ MODEL_PLURAL_NAME }}::class)
    ->assertActionVisible($action);
})->with({{ EDIT_PAGE_VISIBLE_HEADER_ACTIONS }})->group('action', 'render');

it('cannot render action on the edit page', function (string $action) {
   livewire(List{{ MODEL_PLURAL_NAME }}::class)
    ->assertActionHidden($action);
})->with({{ EDIT_PAGE_HIDDEN_HEADER_ACTIONS }})->group('action', 'render');
